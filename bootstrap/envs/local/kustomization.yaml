apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local environment overlay for KinD
# Deploys the local microservices, metrics-server, and Prometheus/Grafana stack
resources:
  - local-apps.yaml
  - metrics-server.yaml
  - prometheus-stack.yaml
  - loki.yaml
  - promtail.yaml
