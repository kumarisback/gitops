apiVersion: v1
kind: ServiceAccount
metadata:
  name: order-service
  namespace: production

# order-service never calls the Kubernetes API, so it gets no token at all.
automountServiceAccountToken: false
