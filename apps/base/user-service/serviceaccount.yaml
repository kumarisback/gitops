apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-service
  namespace: production

# user-service never calls the Kubernetes API, so it gets no token at all.
automountServiceAccountToken: false
